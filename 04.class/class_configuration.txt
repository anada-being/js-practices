- Memo
  - id
  - title
  - content

  - firstRow(content)
  - onlyRow(content)

- MemoDB
  - db

  - createTable()
  - createMemo(content)
  - getMemos()
  - deleteMemo(id)

- DB
  - db

  - runPromise(query, params)
  - allPromise(query, params)
