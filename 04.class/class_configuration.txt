- Memo
  - id
  - title
  - content

  - #setTitle(content)
  - showTitle()

- MemoDB
  - db

  - createTable()
  - createMemo(content)
  - getMemos()
  - deleteMemo(id)

- DB
  - db

  - runPromise(query, params)
  - allPromise(query, params)
